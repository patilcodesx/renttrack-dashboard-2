# RentTrack Environment Variables

# API Base URL (used when mock mode is disabled)
VITE_API_BASE_URL=http://localhost:8081/api

# Enable mock mode (set to 'false' to use real API)
VITE_USE_MOCK=true
